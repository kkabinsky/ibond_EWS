# ThaiBMA Corporate Bond EWS
# Derived from the imports actually present in the 111 source files.
# Install with:  pip install -r requirements.txt

# --- core numerics -----------------------------------------------------------
numpy>=1.24
pandas>=2.0
scipy>=1.10

# --- models ------------------------------------------------------------------
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
statsmodels>=0.14

# --- explainability ----------------------------------------------------------
shap>=0.44

# --- figures -----------------------------------------------------------------
matplotlib>=3.7

# --- desktop interface -------------------------------------------------------
# the GUI targets the 0.84 API: ft.BoxFit, Dropdown.on_select, ft.DropdownOption
flet>=0.84

# --- optional ----------------------------------------------------------------
# torch      : only the deep-learning hazard path (Path 2)
# python-pptx: only build_presentation.py and add_appendix_slides.py
# requests   : only the iBond client
# yfinance   : only fetch_real.py
torch>=2.0
python-pptx>=0.6
requests>=2.31
yfinance>=0.2
